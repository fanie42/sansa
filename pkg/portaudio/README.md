# Portaudio

## External Depencies
The Go library only provides bindings to the Portaudio library. For it to work, the Portaudio libraries need to be installed already. On Ubuntu:

``` Bash
$ sudo apt install portaudio19-dev
```

Or build libraries from source:
``` Bash
$ TODO
```